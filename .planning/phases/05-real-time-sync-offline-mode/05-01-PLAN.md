---
phase: 05-real-time-sync-offline-mode
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/lib/auth.ts, src/pages/verify-email.tsx, src/contexts/AuthContext.tsx, src/contexts/ProtectedRoute.tsx, src/App.tsx]
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03]
must_haves:
  truths:
    - "Users with unconfirmed emails are redirected to /verify-email instead of showing generic errors"
    - "AuthContext confirms session validity on mount before resolving loading state"
    - "Dashboard doesn't flick to login screen if a valid session exists"
  artifacts:
    - path: "src/pages/verify-email.tsx"
      provides: "Unconfirmed account landing page"
    - path: "src/contexts/AuthContext.tsx"
      provides: "Hardened session verification logic"
  key_links:
    - from: "src/contexts/ProtectedRoute.tsx"
      to: "src/pages/verify-email.tsx"
      via: "redirection on unconfirmed status"
---

<objective>
Harden the authentication flow to prevent redirection loops and provide semantic feedback for unconfirmed email accounts.

Purpose: Resolve the "Dashboard -> Login" loop and ensure users know why they can't access their workspace.
Output: Verification landing page, semantic error parsing, and improved session verification.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-real-time-sync-offline-mode/05-CONTEXT.md
@.planning/phases/05-real-time-sync-offline-mode/RESEARCH.md
@src/contexts/AuthContext.tsx
@src/contexts/ProtectedRoute.tsx
@src/lib/auth.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Semantic Error Parsing & Verify Email Page</name>
  <files>src/lib/auth.ts, src/pages/verify-email.tsx, src/App.tsx</files>
  <action>
    - Update `getAuthErrorMessage` in `src/lib/auth.ts` to detect backend response detail "Email not confirmed" (401 Unauthorized) and return a specific error code or descriptive string.
    - Create `src/pages/verify-email.tsx` using `Plus Jakarta Sans`. It should explain that the account is unverified and provide a "Check your inbox" message.
    - Register the `/verify-email` route in `src/App.tsx`.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Verification page exists and auth errors are correctly parsed.</done>
</task>

<task type="auto">
  <name>Task 2: AuthContext & ProtectedRoute hardening</name>
  <files>src/contexts/AuthContext.tsx, src/contexts/ProtectedRoute.tsx</files>
  <action>
    - Update `AuthContext.tsx` to ensure `refreshUser` (calling `/me`) is executed on mount and completion is awaited before setting `loading` to `false`. 
    - Ensure `user` object includes a `confirmed` (or similar) field from backend.
    - Update `ProtectedRoute.tsx` to wait for `loading === false`.
    - If `user` is present but not confirmed, redirect to `/verify-email`.
    - If `user` is null after loading, redirect to `/login`.
    - This prevents the race condition where `localStorage` has a user but the session is invalid or unconfirmed, causing the "Dashboard -> Login" loop.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Redirection loop resolved and unconfirmed users are blocked from ProtectedRoute.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Auth API | Trusting session validity from server |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | ProtectedRoute | mitigate | Ensure no dashboard content renders until loading is false and user is verified. |
| T-05-02 | Denial of Service | AuthContext | mitigate | Handle 401/403 errors gracefully during mount verification to prevent crash loops. |
</threat_model>

<verification>
- Run `npm run typecheck` to ensure no interface mismatches.
- Manual check: Log in with an unconfirmed account (if possible via mock/dev) and verify redirect to `/verify-email`.
</verification>

<success_criteria>
- Unconfirmed users see the verification page.
- Dashboard access is strictly guarded by session verification.
- No "stale session" redirection loops.
</success_criteria>

<output>
After completion, create .planning/phases/05-real-time-sync-offline-mode/05-01-SUMMARY.md
</output>
