---
phase: 05-real-time-sync-offline-mode
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [src/components/audit-trail.tsx, src/lib/audit-trail-helpers.tsx, src/components/new-task-modal.tsx, src/components/task-detail-drawer.tsx, src/hooks/useTasks.ts, src/hooks/useProjects.ts, src/hooks/useTags.ts]
autonomous: true
requirements: [UX-01, UX-02, UX-03, UX-04]
must_haves:
  truths:
    - "Audit Trail is free of LOGIN/LOGOUT noise"
    - "Audit Trail renders without ReferenceError: Skeleton is not defined"
    - "Save/Update buttons are disabled if no changes are detected"
    - "Success toasts include the item name (e.g., Task 'Name' created)"
  artifacts:
    - path: "src/components/audit-trail.tsx"
      provides: "Bug-free historical timeline"
    - path: "src/lib/audit-trail-helpers.tsx"
      provides: "Event filtering logic"
  key_links:
    - from: "src/components/new-task-modal.tsx"
      to: "initial state"
      via: "dirty check comparison"
---

<objective>
Resolve UI bugs, implement dirty checks for save interactions, and refine the Audit Trail for better historical transparency.

Purpose: Improve signal-to-noise ratio in logs and prevent redundant API calls for unchanged data.
Output: Refined audit logs, smarter save buttons, and contextual feedback.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-real-time-sync-offline-mode/05-CONTEXT.md
@src/components/audit-trail.tsx
@src/lib/audit-trail-helpers.tsx
@src/components/new-task-modal.tsx
@src/components/task-detail-drawer.tsx
@src/hooks/useTasks.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit Trail Refinement & Skeleton Fix</name>
  <files>src/components/audit-trail.tsx, src/lib/audit-trail-helpers.tsx</files>
  <action>
    - Add `import { Skeleton } from "@/components/ui/skeleton"` to `src/components/audit-trail.tsx` to fix the ReferenceError.
    - Update `isExcluded` in `src/lib/audit-trail-helpers.tsx` to strictly filter out `LOGIN` and `LOGOUT` actions.
    - Ensure `AuditTrail` renders natural language descriptions focused on task lifecycles (Created, Updated, Completed).
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Audit trail is bug-free and clean.</done>
</task>

<task type="auto">
  <name>Task 2: Global Dirty Checks for Save Buttons</name>
  <files>src/components/new-task-modal.tsx, src/components/task-detail-drawer.tsx</files>
  <action>
    - In `NewTaskModal.tsx`, implement an `isDirty` check. Disable the "Create Task" button if all fields are in their initial state (or empty).
    - In `TaskDetailDrawer.tsx`, ensure the "Update" button is disabled if `hasChanges` is false.
    - Reference: `useTaskDrawerState` already has logic that should be utilized for this.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>Buttons are disabled when no changes are present.</done>
</task>

<task type="auto">
  <name>Task 3: Contextual Toasts in Hooks</name>
  <files>src/hooks/useTasks.ts, src/hooks/useProjects.ts, src/hooks/useTags.ts</files>
  <action>
    - Update the `onSuccess` handlers in `useTasks.ts`, `useProjects.ts`, and `useTags.ts`.
    - Modify the toast messages to include the name of the item created/updated (e.g., `Task "${data.title}" created`).
    - Use variables from the mutation response or variables passed to the mutation.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Toasts provide specific feedback with item names.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client UI → User | Displaying historical events |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Tampering | Audit Trail | accept | Audit trail is read-only for users; tampering happens at DB level. |
</threat_model>

<verification>
- Verify `Skeleton` error is gone in the UI.
- Verify `Login/Logout` events no longer appear in Audit Trail.
- Verify "Update" button is disabled in Task Drawer until a field is changed.
</verification>

<success_criteria>
- No console errors in Audit Trail.
- Clean activity log.
- Better UX for save actions.
</success_criteria>

<output>
After completion, create .planning/phases/05-real-time-sync-offline-mode/05-03-SUMMARY.md
</output>
