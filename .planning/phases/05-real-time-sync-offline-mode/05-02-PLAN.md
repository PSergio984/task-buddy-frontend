---
phase: 05-real-time-sync-offline-mode
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified: [package.json, package-lock.json, src/main.tsx, src/hooks/useTasks.ts, src/hooks/useProjects.ts, src/hooks/useTags.ts]
autonomous: false
requirements: [SYNC-01, SYNC-02]
must_haves:
  truths:
    - "Application remains functional (viewing tasks/projects) when offline"
    - "Mutations reflect in the UI immediately before server confirmation"
    - "Sync conflicts are resolved via Last-Write-Wins (accepts potential data loss for simplicity)"
  artifacts:
    - path: "src/main.tsx"
      provides: "TanStack Query Persistence configuration"
    - path: "package.json"
      provides: "New dependencies for sync layer"
  key_links:
    - from: "TanStack Query"
      to: "IndexedDB"
      via: "@tanstack/react-query-persist-client"
---

<objective>
Implement real-time synchronization and offline caching for core workspace entities using TanStack Query and IndexedDB.

Purpose: Enable offline productivity and high-velocity interactions through optimistic updates.
Output: Persistent client-side cache and optimistic mutation hooks.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-real-time-sync-offline-mode/05-CONTEXT.md
@.planning/phases/05-real-time-sync-offline-mode/RESEARCH.md
@src/main.tsx
@src/hooks/useTasks.ts
@src/hooks/useProjects.ts
@src/hooks/useTags.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: TanStack Query Persistence (IndexedDB)</name>
  <files>package.json, package-lock.json, src/main.tsx</files>
  <action>
    - Install necessary dependencies: `@tanstack/react-query-persist-client` and `idb-keyval`.
    - In `src/main.tsx`, configure the `PersistQueryClientProvider`.
    - Use `experimental_createPersister` with `idb-keyval` for IndexedDB storage.
    - Set `gcTime` and `staleTime` to appropriate values (e.g., 24h for gcTime) to allow offline access.
    - Ensure `AuditTrail` queries are excluded from persistence as per RESEARCH.md (audit logs remain online-only).
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Persistence layer is initialized and wrapping the application.</done>
</task>

<task type="auto">
  <name>Task 2: Optimistic UI for Core Entities (Tasks, Projects, Tags)</name>
  <files>src/hooks/useTasks.ts, src/hooks/useProjects.ts, src/hooks/useTags.ts</files>
  <action>
    - Implement optimistic updates for `useTasks.ts` (Tasks/Subtasks), `useProjects.ts`, and `useTags.ts`.
    - Pattern (per RESEARCH.md): In `onMutate`, cancel outgoing queries, snapshot current data, and update the cache immediately with the new item.
    - In `onError`, rollback to the snapshot.
    - In `onSettled`, invalidate the query to sync with server truth.
    - Cover create, update, delete, and toggle/reorder actions for all three entity types.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>Core workspace mutations feel instantaneous through optimistic UI.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify Optimistic UI and Offline Persistence</name>
  <what-built>Optimistic mutation hooks and IndexedDB persistence layer.</what-built>
  <how-to-verify>
    1. **Persistence Check:**
       - Open the app, navigate to Dashboard.
       - Close the tab, disconnect from network.
       - Reopen the app (offline) -> Verify tasks and projects are still visible from cache.
    2. **Optimistic UI Check (Tasks/Projects/Tags):**
       - Connect to network.
       - Toggle a task -> Verify it checks/unchecks immediately.
       - Create a project -> Verify it appears in the sidebar before the "Success" toast.
       - Update a tag color -> Verify UI reflects the change immediately.
    3. **Resilience Check:**
       - Disconnect network.
       - Toggle a task -> Verify UI state remains in the toggled state.
       - Reconnect network -> Verify the task state persists and matches the server truth.
  </how-to-verify>
  <action>Verify the persistence and optimistic UI behavior as described in the verification steps.</action>
  <verify>Human verification of visual and offline behavior.</verify>
  <done>UI remains responsive and functional across network transitions.</done>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → IndexedDB | Local storage of sensitive task data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Information Disclosure | IndexedDB | accept | Data is per-device/per-user profile; standard browser storage isolation applies. |
| T-05-05 | Tampering | Offline Cache | mitigate | Server truth always overrides local cache on sync (Last-Write-Wins). |
</threat_model>

<verification>
- Run `npm run typecheck`.
- Manual verification per Task 3 checkpoint.
</verification>

<success_criteria>
- Core entities (Tasks/Projects/Tags) are cached offline.
- Interaction latency for mutations is effectively zero.
- Resolve sync conflicts via Last-Write-Wins (acknowledged data loss risk).
</success_criteria>

<output>
After completion, create .planning/phases/05-real-time-sync-offline-mode/05-02-SUMMARY.md
</output>
