---
phase: 01-frontend-rework
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/layout/main-layout.tsx
  - src/App.tsx
  - src/components/sidebar.tsx
  - src/index.css
  - tailwind.config.js
  - src/pages/DashboardDemo.tsx
  - src/pages/ProfilePage.tsx
  - src/pages/AuditLogsPage.tsx
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-04
  - UI-05
must_haves:
  truths:
    - "Sidebar and TopNav persist without re-rendering across route transitions (per UI-01)"
    - "Sidebar supports expanded (280px) and collapsed (72px) states with tooltips (per UI-02)"
    - "Typography uses Plus Jakarta Sans and follows the UI-SPEC spacing scale (per UI-04)"
  artifacts:
    - path: "src/components/layout/main-layout.tsx"
      provides: "Main persistent layout wrapper"
    - path: "src/components/sidebar.tsx"
      provides: "Collapsible sidebar with icon-only collapsed state"
  key_links:
    - from: "src/App.tsx"
      to: "src/components/layout/main-layout.tsx"
      via: "React Router Outlet"
      pattern: "<Route element={<MainLayout />}>"
---

<objective>
Refactor the core application layout to achieve persistence across page transitions and modernize the sidebar UX. This plan establishes the "MainLayout" pattern and implements the design system foundation (typography, spacing, card refinements).

Purpose: Fixes unmounting navigation (D-02), implements professional typography (D-03), and establishes the collapsible sidebar (D-02).
Output: New `MainLayout` component, updated `App.tsx` structure, and a refined, collapsible `Sidebar`.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-frontend-rework/01-CONTEXT.md
@.planning/phases/01-frontend-rework/01-UI-SPEC.md
@.planning/phases/01-frontend-rework/01-RESEARCH.md
@src/App.tsx
@src/components/sidebar.tsx
@src/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Establish Design System Foundation</name>
  <files>src/index.css, tailwind.config.js</files>
  <action>
    1. Update `src/index.css` to import and apply **Plus Jakarta Sans** as the primary font (per UI-SPEC). Replace any "AI slop" fonts like Caveat or Quicksand.
    2. **Consolidate font weights to 400 and 700 per UI-SPEC Typography rules.**
    3. Update `tailwind.config.js` to set the default border-radius to `1rem` (16px) for cards/containers (per UI-SPEC UI-05).
    4. Ensure the spacing scale (multiples of 4) is respected in Tailwind configuration if necessary, though standard Tailwind usually follows this.
    Use the `ui-ux-pro-max` skill for design intelligence to ensure the CSS application is professional.
  </action>
  <verify>
    <automated>grep -q "Plus Jakarta Sans" src/index.css</automated>
  </verify>
  <done>Design system foundation (font, radius, weights) applied.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Persistent MainLayout</name>
  <files>src/components/layout/main-layout.tsx, src/App.tsx, src/pages/DashboardDemo.tsx, src/pages/ProfilePage.tsx, src/pages/AuditLogsPage.tsx</files>
  <action>
    1. Create `src/components/layout/main-layout.tsx`. Use the `MainLayout` pattern from RESEARCH.md. It MUST include `TopNav`, `Sidebar`, and `<Outlet />`.
    2. In `src/App.tsx`, remove existing internal layout wrappers (like `DashboardLayout`). 
    3. Restructure routes so that all protected pages (`/dashboard`, `/profile`, `/audit-logs`) are children of a single `<Route element={<MainLayout />}>`. 
    4. **Remove `<TopNav />` and `<Sidebar />` from `src/pages/DashboardDemo.tsx`, `src/pages/ProfilePage.tsx`, and `src/pages/AuditLogsPage.tsx`** to ensure shell components do not re-mount when navigating between these routes (per UI-01).
  </action>
  <verify>
    <automated>grep -q "Outlet" src/components/layout/main-layout.tsx && grep -q "MainLayout" src/App.tsx</automated>
  </verify>
  <done>Persistent layout structure implemented via MainLayout wrapper; redundant shell components removed from pages.</done>
</task>

<task type="auto">
  <name>Task 3: Refactor Sidebar for Collapsibility and Cleanup</name>
  <files>src/components/sidebar.tsx</files>
  <action>
    1. Modify `src/components/sidebar.tsx` to support `isCollapsed` state (per UI-02).
    2. **Expanded:** 280px wide, Icon + Label.
    3. **Collapsed:** 72px wide, Icon only + Tooltips on hover.
    4. Transition: 200ms ease-in-out for width and opacity changes.
    5. **Cleanup:** Remove redundant user profile info and the "Logout/Disconnect" button from the sidebar (per UI-03).
    6. Use `lucide-react` icons (e.g., `ChevronLeft`, `ChevronRight`) for the toggle trigger.
    7. Use the `ui-ux-pro-max` skill to refine the sidebar's aesthetics and interaction.
  </action>
  <verify>
    <automated>grep -q "isCollapsed" src/components/sidebar.tsx && ! grep -q "Disconnect Session" src/components/sidebar.tsx && ! grep -q "<TopNav" src/pages/ProfilePage.tsx && ! grep -q "<Sidebar" src/pages/DashboardDemo.tsx && ! grep -q "<TopNav" src/pages/AuditLogsPage.tsx</automated>
  </verify>
  <done>Sidebar is collapsible, clean, and follows UI-SPEC dimensions. Pages are confirmed clean of leaky shell components.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Layout | UI shell rendering |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-01 | Information Disclosure | `App.tsx` | mitigate | Ensure `ProtectedRoute` still wraps the routes within `MainLayout` to prevent unauthorized access to the shell. |
</threat_model>

<verification>
1. Navigate between Dashboard and Profile; verify Sidebar/Nav do not flicker or animate.
2. Toggle Sidebar collapse; verify width changes and labels hide/show with transition.
3. Verify Sidebar contains no user profile/logout buttons.
4. Verify individual page files do not contain Sidebar or TopNav imports/usage.
</verification>

<success_criteria>
- Persistent layout achieved.
- Sidebar collapses according to UI-SPEC.
- Typography updated to Plus Jakarta Sans with 400/700 weights.
- Individual pages are stripped of shell components.
</success_criteria>

<output>
After completion, create `.planning/phases/01-frontend-rework/01-01-SUMMARY.md`
</output>
