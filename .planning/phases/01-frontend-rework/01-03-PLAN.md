---
phase: 01-frontend-rework
plan: 03
type: execute
wave: 3
depends_on: ["01", "02"]
files_modified:
  - src/hooks/useApi.ts
  - tests/layout.spec.ts
autonomous: true
requirements:
  - INT-01
  - VERI-01
  - VERI-02
must_haves:
  truths:
    - "API requests match backend CRUD standards (per INT-01)"
    - "E2E tests prove shell persistence across routes (per VERI-01)"
    - "UI style compliance check passes (per VERI-02)"
  artifacts:
    - path: "src/hooks/useApi.ts"
      provides: "Backend-aligned API hooks"
    - path: "tests/layout.spec.ts"
      provides: "Automated layout persistence test"
  key_links:
    - from: "src/hooks/useApi.ts"
      to: "Backend API"
      via: "HTTP requests"
      pattern: "fetch"
---

<objective>
Align data fetching with backend schema changes and verify the persistent layout and design system compliance through automated and manual checks.

Purpose: Ensures data integrity (D-05) and validates the new architectural patterns.
Output: Backend-synced `useApi.ts` and passing Playwright tests in `layout.spec.ts`.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-frontend-rework/01-CONTEXT.md
@.planning/phases/01-frontend-rework/01-UI-SPEC.md
@.planning/phases/01-frontend-rework/01-RESEARCH.md
@src/hooks/useApi.ts
@tests/auth.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align API Hooks with Backend CRUD</name>
  <files>src/hooks/useApi.ts</files>
  <action>
    Review `src/hooks/useApi.ts`. Align all task-related CRUD operations with the backend's standardized literals and schema (e.g., ensuring `title`, `description`, `status` are correct). Accommodate backend changes where `db.commit()` was decoupled (ensure frontend handles success responses correctly). Run `npm run typecheck` to ensure no interface mismatches.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>API hooks align with backend and pass type verification.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Layout Persistence E2E Test</name>
  <files>tests/layout.spec.ts</files>
  <action>
    Create `tests/layout.spec.ts`. Write a Playwright test that:
    1. Authenticates and visits `/dashboard`.
    2. Navigates to `/profile` using the Sidebar.
    3. Asserts that the `Sidebar` and `TopNav` elements remain visible and do not undergo a re-mount (check for absence of entry animations or re-rendering artifacts).
    4. Asserts the presence of the new copy ("Create Task", "Log Out").
    5. Verifies the sidebar collapse toggle works.
  </action>
  <verify>
    <automated>npx playwright test tests/layout.spec.ts</automated>
  </verify>
  <done>Automated verification of persistent layout passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Refactored Frontend UX and Layout Persistence</what-built>
  <how-to-verify>
    1. Visit the local dashboard.
    2. Check the Sidebar: is it collapsible? Is redundant user info gone?
    3. Check the TopNav: is it "Create Task" instead of "Forge Task"? Is the font professional?
    4. Check the Landing Page: is the "Watch Demo" button gone? Is the footer year correct?
    5. Check transitions: do the Sidebar and Nav stay static while clicking between routes?
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API Integration | Data sync with backend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | Information Disclosure | `useApi.ts` | mitigate | Ensure error responses do not leak sensitive backend implementation details to the console/UI. |
</threat_model>

<verification>
- `npm run typecheck` passes.
- Playwright `layout.spec.ts` passes.
- Visual sign-off of the new design system (font, spacing, cards).
</verification>

<success_criteria>
- Frontend-Backend integration is seamless.
- Layout persistence is verified automatedly.
- Human verify confirms design system compliance.
</success_criteria>

<output>
After completion, create `.planning/phases/01-frontend-rework/01-03-SUMMARY.md`
</output>
