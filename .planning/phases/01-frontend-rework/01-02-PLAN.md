---
phase: 01-frontend-rework
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - src/components/topnav.tsx
  - src/pages/LandingPage.tsx
  - src/components/dashboard.tsx
  - src/components/new-task-modal.tsx
autonomous: true
requirements:
  - UI-06
  - UI-07
  - UI-08
must_haves:
  truths:
    - "TopNav uses professional copy ('Create Task') and standard labels (per UI-06)"
    - "Landing Page has a dynamic copyright year and no 'Watch Demo' button (per UI-08)"
    - "Loading states use Skeleton screens and spinning icons (per UI-07)"
  artifacts:
    - path: "src/components/topnav.tsx"
      provides: "Header with standard terminology and logout button"
    - path: "src/pages/LandingPage.tsx"
      provides: "Modern landing page with dynamic footer"
  key_links:
    - from: "src/components/topnav.tsx"
      to: "Logout Logic"
      via: "Log Out button"
      pattern: "Log Out"
---

<objective>
Refine typography, remove AI-generated copy, fix landing page issues, and modernize loading states across the application. This plan focuses on implementation of the "Copywriting Contract" and polished "Interaction Contracts" from the UI-SPEC.

Purpose: Implements standard wording (D-03), Landing Page fixes (D-04), and polished loading indicators (D-03).
Output: Updated `TopNav`, `LandingPage`, and optimized loading components.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-frontend-rework/01-CONTEXT.md
@.planning/phases/01-frontend-rework/01-UI-SPEC.md
@src/components/topnav.tsx
@src/pages/LandingPage.tsx
@src/components/dashboard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean TopNav Copy and Typography</name>
  <files>src/components/topnav.tsx</files>
  <action>
    1. Remove "Operational Status: Optimal" and any "Rise and Shine" greetings.
    2. Change "Forge Task" button to "Create Task" (per UI-06).
    3. Ensure the font classes on headings match the "Plus Jakarta Sans" professional style.
    4. Ensure the user profile dropdown has a clear "Log Out" label (per UI-SPEC).
    5. Add appropriate `lucide-react` icons to navigation items and the "Create Task" button.
    6. Use the `ui-ux-pro-max` skill for design intelligence.
  </action>
  <verify>
    <automated>grep -q "Create Task" src/components/topnav.tsx && grep -q "Log Out" src/components/topnav.tsx</automated>
  </verify>
  <done>TopNav copy and typography are professional and icon-rich.</done>
</task>

<task type="auto">
  <name>Task 2: Refine Landing Page</name>
  <files>src/pages/LandingPage.tsx</files>
  <action>
    1. Remove the "Watch Demo" button from the hero section (per UI-08).
    2. Update the footer copyright to be dynamic: `{new Date().getFullYear()}`.
    3. Slightly refine section spacing to match the 4px-multiples scale (per UI-04).
    4. Use the `ui-ux-pro-max` skill to ensure the landing page remains aesthetically pleasing while removing slop.
  </action>
  <verify>
    <automated>! grep -q "Watch Demo" src/pages/LandingPage.tsx && grep -q "getFullYear()" src/pages/LandingPage.tsx</automated>
  </verify>
  <done>Landing page is cleaned up and has dynamic footer.</done>
</task>

<task type="auto">
  <name>Task 3: Modernize Loading States and Interaction Feedback</name>
  <files>src/components/dashboard.tsx, src/components/new-task-modal.tsx</files>
  <action>
    1. Replace generic "Loading..." text with shadcn `Skeleton` loaders for the Dashboard grid (per UI-SPEC Interaction Contract 3).
    2. Use `Loader2` from `lucide-react` with `animate-spin` for button loading states in `NewTaskModal`.
    3. Verify empty state copy matches the UI-SPEC ("No tasks yet", "Your list is clear...").
    4. Use the `ui-ux-pro-max` skill for polished interaction design.
  </action>
  <verify>
    <automated>grep -q "Skeleton" src/components/dashboard.tsx && grep -q "Loader2" src/components/new-task-modal.tsx</automated>
  </verify>
  <done>Loading states are polished with Skeletons and animated icons.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client UI | Handling user sessions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Information Disclosure | `TopNav.tsx` | mitigate | Ensure user name/email in TopNav is handled securely via the Auth context. |
</threat_model>

<verification>
1. Dashboard initial load shows Skeleton screens.
2. Clicking "Create Task" shows a modal with professional copy.
3. Landing Page footer shows current year.
</verification>

<success_criteria>
- AI slop copy removed entirely.
- Dynamic footer functional.
- Polished loaders implemented.
</success_criteria>

<output>
After completion, create `.planning/phases/01-frontend-rework/01-02-SUMMARY.md`
</output>
