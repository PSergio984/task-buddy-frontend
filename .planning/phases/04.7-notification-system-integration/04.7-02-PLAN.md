---
phase: 04.7-notification-system-integration
plan: 02
type: execute
wave: 2
depends_on: [04.7-01]
files_modified: [src/components/notification-bell.tsx, src/components/notification-item.tsx, src/components/topnav.tsx, src/pages/ProfilePage.tsx]
autonomous: true
---

<objective>
Implement the UI components for notifications, including the notification bell in the TopNav, the notification item list within the Popover, and the push notification toggle in the Profile Settings.

Purpose: Provide user-facing controls and displays for notifications.
Output: NotificationBell, NotificationItem, and updated TopNav/ProfilePage.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@../task-buddy-frontend/.planning/phases/04.7-notification-system-integration/04.7-CONTEXT.md
@../task-buddy-frontend/.planning/phases/04.7-notification-system-integration/04.7-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create NotificationItem Component</name>
  <files>src/components/notification-item.tsx</files>
  <action>
    Create `src/components/notification-item.tsx`:
    1. Render notification title, message, and relative time (using `date-fns`).
    2. Add icon mapping for different notification types.
    3. Apply styles for read/unread states per UI-SPEC.
    4. Handle click to mark as read and navigate.
  </action>
  <verify>
    <automated>Check if component renders correctly with mock data.</automated>
  </verify>
  <done>NotificationItem component implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Create NotificationBell Component</name>
  <files>src/components/notification-bell.tsx</files>
  <action>
    Create `src/components/notification-bell.tsx`:
    1. Use `Popover` from `@/components/ui/popover`.
    2. Use `Bell` icon from `lucide-react`.
    3. Implement the unread badge with `unreadCount`.
    4. Render a list of `NotificationItem` components inside the popover.
    5. Add "Mark all as read" button in the header.
    6. Add empty state handling.
  </action>
  <verify>
    <automated>Check if Popover opens and displays notifications.</automated>
  </verify>
  <done>NotificationBell component implemented with badge and dropdown.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate Bell into TopNav</name>
  <files>src/components/topnav.tsx</files>
  <action>
    Modify `src/components/topnav.tsx`:
    1. Import `NotificationBell`.
    2. Place `NotificationBell` to the left of the user profile dropdown.
    3. Ensure consistent spacing and alignment.
  </action>
  <verify>
    <automated>Visual check of TopNav layout.</automated>
  </verify>
  <done>NotificationBell integrated into TopNav.</done>
</task>

<task type="auto">
  <name>Task 4: Add Push Toggle to ProfilePage</name>
  <files>src/pages/ProfilePage.tsx</files>
  <action>
    Modify `src/pages/ProfilePage.tsx`:
    1. Add a new section/card for "Preferences" or "Notifications".
    2. Add a `Switch` (from Radix/Shadcn) for "Push Notifications".
    3. Add descriptive text per UI-SPEC.
    4. Wire up the toggle state (initially just UI state, logic in Plan 03).
  </action>
  <verify>
    <automated>Visual check of ProfilePage.</automated>
  </verify>
  <done>Push notification toggle added to Profile Settings.</done>
</task>

</tasks>

<verification>
1. `NotificationBell` is visible in `TopNav`.
2. `Popover` opens on click.
3. `ProfilePage` shows the new notification section.
</verification>

<success_criteria>
- User can see and interact with the notification bell.
- Notification items are styled according to the UI-SPEC.
- Profile page has a dedicated section for notification settings.
</success_criteria>
