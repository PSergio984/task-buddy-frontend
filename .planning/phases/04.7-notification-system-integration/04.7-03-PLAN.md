---
phase: 04.7-notification-system-integration
plan: 03
type: execute
wave: 3
depends_on: [04.7-02]
files_modified: [src/App.tsx, src/pages/ProfilePage.tsx, src/hooks/useNotifications.ts]
autonomous: true
---

<objective>
Connect the notification UI with the backend logic, implement real-time toast feedback for high-priority reminders, and handle the browser push registration flow.

Purpose: Finalize the notification system functionality and user experience.
Output: Push registration logic, Toast triggers, and verified end-to-end flow.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@../task-buddy-frontend/.planning/phases/04.7-notification-system-integration/04.7-CONTEXT.md
@../task-buddy-frontend/.planning/phases/04.7-notification-system-integration/04.7-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Push Registration Logic</name>
  <files>src/pages/ProfilePage.tsx, src/hooks/useNotifications.ts</files>
  <action>
    1. Implement logic to check current push permission on `ProfilePage` load.
    2. Implement `handleTogglePush`:
       - If enabling: Call `Notification.requestPermission()`. If granted, get `swRegistration.pushManager.subscribe` (using backend VAPID key) and send to backend.
       - If disabling: Unsubscribe or just update local state/backend preferences.
    3. Update `useNotifications.ts` to include a helper for VAPID key retrieval if needed (or just use a constant from config).
  </action>
  <verify>
    <automated>Check console logs for subscription object when enabling.</automated>
  </verify>
  <done>Push registration logic implemented and wired to Profile toggle.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Real-time Toast Logic</name>
  <files>src/App.tsx</files>
  <action>
    Modify `src/App.tsx` or create a `NotificationWatcher` component:
    1. Use `useNotifications` hook to monitor for new notifications.
    2. Keep track of already "toasted" notification IDs in local state or ref.
    3. If a new high-priority notification (`REMINDER_DUE`, `REMINDER_OVERDUE`) is detected, trigger `toast()`.
  </action>
  <verify>
    <automated>Simulate a new notification and check if toast appears.</automated>
  </verify>
  <done>Real-time toast feedback implemented for high-priority reminders.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Mark All as Read</name>
  <files>src/hooks/useNotifications.ts, src/components/notification-bell.tsx</files>
  <action>
    1. Add `markAllAsRead` mutation to `useNotifications.ts` (requires backend support or multiple PATCH calls if backend doesn't have a bulk endpoint).
    2. *Note: Backend currently only has `PATCH /{id}/read`. If no bulk endpoint exists, iterate over unread IDs.*
    3. Wire the button in `NotificationBell` to this mutation.
  </action>
  <verify>
    <automated>Click "Mark all as read" and verify badge clears.</automated>
  </verify>
  <done>Bulk "Mark as read" functionality implemented.</done>
</task>

</tasks>

<verification>
1. Toggling push in Profile requests permission.
2. New notifications appear in the bell.
3. High-priority notifications trigger a toast.
4. "Mark all as read" clears the badge.
</verification>

<success_criteria>
- End-to-end notification flow is functional.
- Push subscriptions are correctly sent to the backend.
- UI state is kept in sync via TanStack Query.
</success_criteria>
