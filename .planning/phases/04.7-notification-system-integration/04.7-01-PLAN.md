---
phase: 04.7-notification-system-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [vite.config.ts, src/sw.ts, src/lib/api.ts, src/hooks/useNotifications.ts]
autonomous: true
---

<objective>
Setup the PWA infrastructure and API layer for notifications. This includes switching to the `injectManifest` strategy for service workers to support background push listeners and creating the hooks for fetching notification data.

Purpose: Enable background notifications and provide data fetching capabilities.
Output: Custom service worker, updated PWA config, and notification hooks.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@../task-buddy-frontend/.planning/phases/04.7-notification-system-integration/04.7-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update PWA Configuration</name>
  <files>vite.config.ts</files>
  <action>
    Modify `vite.config.ts`:
    1. Set `strategies: 'injectManifest'`.
    2. Set `srcDir: 'src'`.
    3. Set `filename: 'sw.ts'`.
    4. Ensure `VitePWA` options are correctly aligned for manifest injection.
  </action>
  <verify>
    <automated>npm run build (Check if dist/sw.js is generated)</automated>
  </verify>
  <done>Vite PWA configured for custom service worker.</done>
</task>

<task type="auto">
  <name>Task 2: Create Custom Service Worker</name>
  <files>src/sw.ts</files>
  <action>
    Create `src/sw.ts`:
    1. Import `precacheAndRoute` from `workbox-precaching`.
    2. Add `precacheAndRoute(self.__WB_MANIFEST)`.
    3. Add a `push` event listener to show browser notifications using `self.registration.showNotification`.
    4. Add a `notificationclick` event listener to handle clicks and open the `action_url`.
  </action>
  <verify>
    <automated>Check if src/sw.ts exists and has required listeners.</automated>
  </verify>
  <done>Custom service worker implemented with push support.</done>
</task>

<task type="auto">
  <name>Task 3: Update API Layer</name>
  <files>src/lib/api.ts</files>
  <action>
    Add notification-related API calls:
    1. `getNotifications(params)`: GET `/api/v1/notifications/`.
    2. `markNotificationRead(id)`: PATCH `/api/v1/notifications/{id}/read`.
    3. `registerPushSubscription(subscription)`: POST `/api/v1/notifications/push-subscription`.
  </action>
  <verify>
    <automated>Check for new exports in src/lib/api.ts.</automated>
  </verify>
  <done>API layer updated for notifications.</done>
</task>

<task type="auto">
  <name>Task 4: Create Notification Hooks</name>
  <files>src/hooks/useNotifications.ts</files>
  <action>
    Create `src/hooks/useNotifications.ts`:
    1. `useNotifications`: Fetch notifications using TanStack Query with `refetchInterval: 120000` and `refetchOnWindowFocus: true`.
    2. `useMarkRead`: Mutation to mark a notification as read and invalidate the query.
    3. `useRegisterPush`: Mutation to register a push subscription.
    4. Derived state: `unreadCount`.
  </action>
  <verify>
    <automated>Check for hook exports and TanStack Query integration.</automated>
  </verify>
  <done>TanStack Query hooks implemented for notifications.</done>
</task>

</tasks>

<verification>
1. `npm run build` completes successfully.
2. `src/sw.ts` is present and correctly referenced in `vite.config.ts`.
3. Hooks are importable and functional.
</verification>

<success_criteria>
- Frontend can fetch notifications via hooks.
- PWA is ready for push notifications (requires build to fully verify).
</success_criteria>
