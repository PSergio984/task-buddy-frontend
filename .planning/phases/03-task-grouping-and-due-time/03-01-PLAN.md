---
phase: 3
plan: 01
type: auto
autonomous: true
wave: 1
depends_on: []
files:
  - src/lib/api.ts
  - src/hooks/useProjects.ts
  - src/components/create-project-modal.tsx
  - src/components/sidebar.tsx
  - src/components/dashboard.tsx
  - src/components/new-task-modal.tsx
  - src/pages/TaskDetailPage.tsx
  - src/contexts/FilterContext.tsx
requirements: ["REFACTOR-GROUPS-TO-PROJECTS"]
---

# Plan: Refactor "Group" to "Project"

Comprehensive refactor of the frontend to replace all instances of "Group" with "Project" and "group" with "project" where appropriate to align with backend terminology and product vision.

## Tasks

<task type="auto">
  <name>Task 1: Update API library</name>
  <files>src/lib/api.ts</files>
  <action>
    - Update `Group` type to `Project`.
    - Update `Task` type: `group_id` -> `project_id`, `group` -> `project`.
    - Update `tasksApi`: rename `group_id` parameters to `project_id`.
    - Update `groupsApi` to `projectsApi`: update endpoints from `/api/v1/groups/` to `/api/v1/projects/`.
  </action>
  <verify>Check for type errors in api.ts</verify>
</task>

<task type="auto">
  <name>Task 2: Update useProjects hook</name>
  <files>src/hooks/useProjects.ts</files>
  <action>
    - Rename `useGroups` -> `useProjects`.
    - Rename `useCreateGroup` -> `useCreateProject`.
    - Rename `useUpdateGroup` -> `useUpdateProject`.
    - Rename `useDeleteGroup` -> `useDeleteProject`.
    - Update query keys from `["groups"]` to `["projects"]`.
    - Update logic to use `projectsApi`.
  </action>
  <verify>Verify hook names and imports.</verify>
</task>

<task type="auto">
  <name>Task 3: Refactor Components</name>
  <files>
    src/components/create-project-modal.tsx,
    src/components/sidebar.tsx,
    src/components/dashboard.tsx,
    src/components/new-task-modal.tsx
  </files>
  <action>
    - Rename `CreateGroupModal` (if exists) or update labels in `create-project-modal.tsx`.
    - Update `sidebar.tsx`: Change "Workspaces" label to "Projects", update variable names, use `useProjects`.
    - Update `dashboard.tsx`: Update variable names and logic.
    - Update `new-task-modal.tsx`: Update labels, rename `groupId` -> `projectId`, use `useProjects`.
  </action>
  <verify>Check component rendering and props.</verify>
</task>

<task type="auto">
  <name>Task 4: Update Pages and Contexts</name>
  <files>src/pages/TaskDetailPage.tsx, src/contexts/FilterContext.tsx</files>
  <action>
    - `TaskDetailPage.tsx`: Update `task.group` -> `task.project`.
    - `FilterContext.tsx`: Update `activeSidebarFilter` logic if it uses `group:`, rename `groupId` to `projectId` in state.
  </action>
  <verify>Verify page functionality.</verify>
</task>

<task type="auto">
  <name>Task 5: Final Verification</name>
  <action>Run `npm run build` to ensure no type regressions.</action>
  <verify>Successful build.</verify>
</task>
