---
phase: 03.8-sidebar-organization-dnd
plan: 02
type: execute
wave: 2
depends_on: ["03.8-01"]
files_modified: [
  "src/components/sidebar/item-actions.test.tsx",
  "src/components/sidebar.test.tsx",
  "src/components/sidebar/item-actions.tsx",
  "src/components/sidebar.tsx"
]
autonomous: true
requirements: [SIDE-02, SIDE-03, SIDE-04]
must_haves:
  truths:
    - "Projects and Tags have an ellipsis menu on hover"
    - "Editing a project/tag via the menu opens the correct modal with data"
    - "Deleting the active filter resets the view to 'All Tasks'"
  artifacts:
    - path: "src/components/sidebar/item-actions.tsx"
      provides: "Reusable ellipsis action menu"
  key_links:
    - from: "src/components/sidebar/item-actions.tsx"
      to: "src/components/delete-confirmation-modal.tsx"
      via: "Dialog/Modal trigger"
    - from: "src/components/sidebar.tsx"
      to: "src/contexts/FilterContext.tsx"
      via: "setActiveSidebarFilter call on delete"
---

<objective>
Implement CRUD actions (Edit/Delete) for Sidebar Projects and Tags using an ellipsis menu and confirmation modals.

Purpose: Provide a centralized management interface for workspace organization.
Output: Hover-triggered ellipsis menus with Edit and Delete functionality.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@FRONTEND_STANDARDS.md
@src/components/sidebar.tsx
@src/components/delete-confirmation-modal.tsx
@src/components/create-project-modal.tsx
@src/components/create-tag-modal.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 0: Scaffold sidebar CRUD tests</name>
  <files>src/components/sidebar/item-actions.test.tsx, src/components/sidebar.test.tsx</files>
  <action>
    Create Vitest component test scaffolds for sidebar actions:
    1. `src/components/sidebar/item-actions.test.tsx`: Verify that the ellipsis menu renders, opens on click, and triggers callbacks for Edit/Delete.
    2. `src/components/sidebar.test.tsx`: Verify that clicking Delete in the sidebar opens the confirmation modal and that confirming deletion triggers the expected API call and filter reset logic.
  </action>
  <verify>
    <automated>ls src/components/sidebar/item-actions.test.tsx src/components/sidebar.test.tsx</automated>
  </verify>
  <done>Test files exist with failing/placeholder tests.</done>
</task>

<task type="auto">
  <name>Task 1: Create sidebar item actions component</name>
  <files>src/components/sidebar/item-actions.tsx</files>
  <action>
    Create a reusable `SidebarItemActions` component (per D-02):
    1. **Standards Compliance**: Use `Readonly<Props>` for the interface. Ensure the `DropdownMenuTrigger` uses a semantic `<button type="button">` (likely handled by shadcn `Button` as child).
    2. Use `DropdownMenu` from `@/components/ui/dropdown-menu`.
    3. Render a `MoreVertical` (ellipsis) icon as the trigger, visible on hover.
    4. Options: "Edit" (with `Edit2` icon) and "Delete" (with `Trash2` icon, `text-destructive`).
    5. Accept callbacks: `onEdit` and `onDelete`.
    6. Ensure `e.stopPropagation()` and `e.preventDefault()` are handled to avoid triggering sidebar filters when opening the menu.
  </action>
  <verify>
    <automated>npm run test src/components/sidebar/item-actions.test.tsx</automated>
  </verify>
  <done>Ellipsis menu component is ready and passing unit tests.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate Actions and Modals into Sidebar</name>
  <files>src/components/sidebar.tsx</files>
  <action>
    Update `Sidebar.tsx` to handle Project and Tag CRUD (per D-02):
    1. Import `SidebarItemActions` and `DeleteConfirmationModal`.
    2. Add state for `deletingItem`.
    3. In Projects and Tags mapping, replace any existing buttons with `SidebarItemActions`.
    4. Implement `handleEditProject` and `handleEditTag` (setting `editingProject`/`editingTag` and opening modals).
    5. Implement `handleDeleteClick` (setting `deletingItem` to open confirmation modal).
    6. **Standards**: Ensure interactive elements remain semantic and accessible.
  </action>
  <verify>
    <automated>npm run test src/components/sidebar.test.tsx</automated>
  </verify>
  <done>CRUD actions are integrated into the Sidebar UI and pass integration tests.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Delete Logic and Filter Reset</name>
  <files>src/components/sidebar.tsx</files>
  <action>
    Finalize the deletion logic with state cleanup (per D-03):
    1. Implement `handleConfirmDelete`:
       - Call appropriate mutation based on type (project/tag).
    2. Add **Filter Reset Logic** (SIDE-04, per D-03):
       - If deleting active project filter, reset to "all".
       - If deleting active tag filter, reset to null.
    3. Show success toast after deletion.
  </action>
  <verify>
    <automated>npm run test src/components/sidebar.test.tsx</automated>
  </verify>
  <done>Deletion logic is robust and handles filter state correctly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | Delete requests sent to backend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.8-03 | Tampering | `Sidebar.tsx` | mitigate | Ensure `e.stopPropagation` prevents accidental navigation during CRUD actions. |
</threat_model>

<verification>
1. Create a test project -> Edit it -> Changes persist.
2. Select the test project (filter active) -> Delete it -> Sidebar resets to "All Tasks".
3. Verify the same for Tags.
</verification>

<success_criteria>
- SIDE-02: Ellipsis menus present and functional.
- SIDE-03: Edit and Delete work correctly.
- SIDE-04: Filter auto-reset works as expected.
- Strict adherence to `Readonly` props and semantic elements.
</success_criteria>

<output>
After completion, create `.planning/phases/03.8-sidebar-organization-dnd/03.8-02-SUMMARY.md`
</output>
