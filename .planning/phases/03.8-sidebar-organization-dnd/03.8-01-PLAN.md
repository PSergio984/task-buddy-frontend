---
phase: 03.8-sidebar-organization-dnd
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "tests/sidebar-dnd.spec.ts",
  "src/components/sidebar/sortable-item.tsx",
  "src/components/sidebar/sortable-item.test.tsx",
  "src/components/sidebar/grip-handle.tsx",
  "src/components/sidebar.tsx"
]
autonomous: true
requirements: [SIDE-01]
must_haves:
  truths:
    - "Projects can be reordered via drag-and-drop handle"
    - "Tags can be reordered via drag-and-drop handle"
    - "Reordering persists after page refresh"
  artifacts:
    - path: "src/components/sidebar/sortable-item.tsx"
      provides: "DND wrapper for sidebar items"
    - path: "src/components/sidebar/grip-handle.tsx"
      provides: "Reusable grip handle icon"
  key_links:
    - from: "src/components/sidebar.tsx"
      to: "src/hooks/useProjects.ts"
      via: "useReorderProjects hook"
    - from: "src/components/sidebar.tsx"
      to: "src/hooks/useTags.ts"
      via: "useReorderTags hook"
---

<objective>
Implement Drag-and-Drop (DND) reordering for Projects and Tags in the Sidebar using @dnd-kit.

Purpose: Allow users to organize their workspace by reordering navigation items.
Output: Reorderable sidebar sections for Projects and Tags with persistent state.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@FRONTEND_STANDARDS.md
@src/components/sidebar.tsx
@src/hooks/useProjects.ts
@src/hooks/useTags.ts
</context>

<tasks>

<task type="auto">
  <name>Task 0: Scaffold reordering tests</name>
  <files>tests/sidebar-dnd.spec.ts, src/components/sidebar/sortable-item.test.tsx</files>
  <action>
    Create test scaffolds for sidebar reordering (per Nyquist compliance):
    1. `tests/sidebar-dnd.spec.ts`: Playwright E2E test skeleton that mocks API responses for reordering and asserts that `arrayMove` was called/reflected in the UI.
    2. `src/components/sidebar/sortable-item.test.tsx`: Vitest component test skeleton to verify that the sortable wrapper correctly applies `dnd-kit` attributes and renders children.
  </action>
  <verify>
    <automated>ls tests/sidebar-dnd.spec.ts src/components/sidebar/sortable-item.test.tsx</automated>
  </verify>
  <done>Test files exist with failing/placeholder tests.</done>
</task>

<task type="auto">
  <name>Task 1: Create sidebar DND sub-components</name>
  <files>src/components/sidebar/sortable-item.tsx, src/components/sidebar/grip-handle.tsx</files>
  <action>
    Create reusable DND components for the sidebar (per D-01, D-04):
    1. **Standards Compliance**: Use `Readonly<Props>` for all interfaces (FRONTEND_STANDARDS Section 2). Use semantic `<button>` for the grip handle (Section 3).
    2. `grip-handle.tsx`: A component returning the `GripVertical` icon from `lucide-react` wrapped in a semantic `<button type="button" aria-label="Drag to reorder">` with `cursor-grab` styling.
    3. `sortable-item.tsx`: A wrapper component using `useSortable` from `@dnd-kit/sortable`. 
       - It should accept `id`, `children`, and `handle` (optional).
       - Apply `transform` and `transition` styles from `useSortable`.
       - Render children and the grip handle. Ensure `opacity` is reduced during dragging.
       - Use React 19 functional ref patterns.
  </action>
  <verify>
    <automated>npm run test src/components/sidebar/sortable-item.test.tsx</automated>
  </verify>
  <done>DND sub-components pass unit tests and adhere to project standards.</done>
</task>

<task type="auto">
  <name>Task 2: Implement DND reordering for Projects</name>
  <files>src/components/sidebar.tsx</files>
  <action>
    Integrate `@dnd-kit` into the Projects section of `sidebar.tsx` (per D-01):
    1. Initialize `useSensors` with `PointerSensor` (5px activation constraint).
    2. Wrap Projects list in `DndContext` and `SortableContext` with `verticalListSortingStrategy`.
    3. Use `SortableSidebarItem` for each project.
    4. Implement `handleProjectDragEnd` using `arrayMove` and `reorderProjects.mutate`.
    5. **Standard**: Ensure the Projects list container uses semantic list elements (`<ul>`, `<li>`) if possible, or maintains accessible navigation landmarks.
  </action>
  <verify>
    <automated>npx playwright test tests/sidebar-dnd.spec.ts</automated>
  </verify>
  <done>Projects reordering is functional and persistent.</done>
</task>

<task type="auto">
  <name>Task 3: Implement DND reordering for Tags</name>
  <files>src/components/sidebar.tsx</files>
  <action>
    Enable DND for the Tags section in `sidebar.tsx` (per D-01):
    1. Wrap Tags list in `SortableContext` with `rectSortingStrategy` (due to flex-wrap).
    2. Use `SortableSidebarItem` for each tag.
    3. Implement `handleTagDragEnd` calling `reorderTags.mutate`.
    4. Verify that reordering logic handles the `arrayMove` correctly before calling mutation.
  </action>
  <verify>
    <automated>npx playwright test tests/sidebar-dnd.spec.ts</automated>
  </verify>
  <done>Tags reordering is functional and persistent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | Reorder requests sent to backend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.8-01 | Tampering | `projectsApi.reorder` | mitigate | Backend validates that all IDs belong to the authenticated user. |
| T-03.8-02 | Denial of Service | `Sidebar.tsx` | mitigate | Use `activationConstraint` in DND sensors to prevent accidental/spammy drag starts. |
</threat_model>

<verification>
1. Drag a project to a new position -> Refresh -> Position persists.
2. Drag a tag to a new position -> Refresh -> Position persists.
3. Verify that clicking a project/tag to filter still works (drag handle doesn't block click).
</verification>

<success_criteria>
- SIDE-01: Projects and Tags reordering is fully functional.
- Smooth animations during drag.
- Strict adherence to `Readonly` props and semantic elements.
</success_criteria>

<output>
After completion, create `.planning/phases/03.8-sidebar-organization-dnd/03.8-01-SUMMARY.md`
</output>
