# Plan: Phase 4 — Premium UX Hardening & Descriptive History

This phase focuses on visual and functional "hardening" of the Task Buddy UI, moving toward a premium, keyboard-centric experience inspired by Linear and Raycast.

## 🛠️ Task 1: Command Palette & Modal Refinements
**Objective**: Transform the task creation modal into a centered "Command Palette" style with fixed contrast.

- [x] **Component**: `src/components/new-task-modal.tsx`
  - [x] Update `DialogContent` className to `sm:max-w-2xl` and ensure absolute centering.
  - [x] Increase backdrop blur to `backdrop-blur-xl` and overlay opacity.
  - [x] **Fix Contrast**: Update `SelectItem` focus classes to `focus:bg-primary/10 focus:text-primary` (light) and `dark:focus:bg-primary/20 dark:focus:text-accent`.
  - [x] Adjust internal padding and spacing to match the wider palette format.

## 🧹 Task 2: Task Studio Layout Cleanup
**Objective**: Remove redundant buttons and streamline the "Task Studio" workspace.

- [x] **Component**: `src/pages/TasksPage.tsx`
  - [x] Remove the "New Task" button from the page header.
  - [x] Reposition the Sort/Filter container to the right side of the layout.
  - [x] Update header spacing to account for the removed button.

## 🔢 Task 3: Persistent Sorting Logic
**Objective**: Implement actual sorting functionality that persists across sessions.

- [x] **Component**: `src/pages/TasksPage.tsx`
  - [x] Add state for `sortBy` (defaulting to `due_date`).
  - [x] Implement `useEffect` to sync `sortBy` with `localStorage.getItem('tb_sort_preference')`.
  - [x] Create a `sortedTasks` memoized array based on `filteredTasks` and `sortBy`.
  - [x] Replace the static "Sort" button with a `DropdownMenu` containing:
    - `Priority (High-Low)`
    - `Deadline (Soonest)`
    - `Alphabetical`
  - [x] Show an arrow icon (`ChevronUp`/`ChevronDown`) indicating active sort.

## 🛡️ Task 4: TopNav & Account Menu Hardening
**Objective**: Fix transparency issues and remove redundant navigation paths.

- [x] **Component**: `src/components/topnav.tsx`
  - [x] Remove the "History" button from the avatar dropdown.
  - [x] Update dropdown background to `bg-background/98` (almost solid) to fix "messy overlap" issues.
  - [x] Enhance dropdown shadow to `shadow-2xl` and add a subtle `border-primary/10`.

## 📜 Task 5: Refined Activity Timeline
**Objective**: Group logs by date, remove auth noise, and make logs descriptive.

- [x] **Component**: `src/components/audit-trail.tsx`
  - [x] **Filtering**: Update `fetchAuditLog` or filtering logic to exclude actions starting with `LOGIN` or `LOGOUT`.
  - [x] **Grouping**: Implement a helper function to group logs by formatted date (e.g., "Today", "Yesterday", "May 10, 2024").
  - [x] **Descriptive Details**: Create a mapping function for `log.action` to produce human-readable strings:
    - `create_task` -> "Orchestrated task: **{title}**"
    - `update_task` -> "Refined task: **{title}**"
  - [x] Update the UI loop to render date headers between groups.

## 🚦 Verification Loop
- [x] **Manual UX Check**:
  - [x] Hover over Project/Priority in Create Task modal — text remains clearly visible.
  - [x] Change Sort in Task Studio, refresh page — sort remains the same.
  - [x] Verify Activity history shows "Today" header and no login logs.
  - [x] Verify TopNav dropdown looks "solid" and elevated over dashboard cards.
- [x] **Build Check**: `npm run build` must succeed.

---
*Status: Ready for execution*
