# UI-SPEC Phase 4.1: Advanced UI/UX & Feature Refinement

## 🎨 Visual Persona: The "Specialized Hub"
Moving toward a high-velocity task management interface (TickTick/Linear inspired).

## 📐 Sidebar Reorganization (src/components/sidebar.tsx)

### 1. The Boxed Toggle (Image 2/3 Style)
- **Position**: Top right of the sidebar container.
- **Visual**: A boxed square (`h-10 w-10`) with `rounded-xl`, `border border-border`, and `bg-background/80 backdrop-blur-md`.
- **Animation**: On collapse, the icon shifts and potentially rotates to indicate direction.

### 2. Smart Lists & Navigation
- **Top Section (Smart Lists)**: 
  - `Today` (Calendar icon), `Next 7 Days` (Calendar-range), `Inbox` (Inbox icon).
- **Navigation (renamed)**: 
  - `Overview` (Dashboard), `Tasks` (renamed from Task Studio), `Activity` (History).
- **Projects (Lists)**:
  - Add a dedicated section icon for the "Projects" header.
  - Hover states: Add a more tactile "boxed" hover background.
- **Tags (Focus Tags)**:
  - Add a dedicated section icon (`Tag` Lucide icon) to the header.
  - Improve the layout to be more compact or list-like if many tags exist.

## 📋 Task Detail Drawer (src/components/task-detail-drawer.tsx)

### 1. Side-Panel Layout
- **Style**: A right-side drawer (`Sheet` component style).
- **Width**: `w-full max-w-[800px]`.
- **Surface**: `bg-background/95 backdrop-blur-2xl` with a subtle primary border on the left edge.

### 2. Internal Structure (Image 5 Style)
- **Content Column (Left - 65%)**:
  - Task Title (Large, bold).
  - Description area (Markdown-ready).
  - Subtasks list with inline add.
  - Activity/Comments section.
- **Meta Column (Right - 35%)**:
  - Vertical stack of labeled rows: `Project`, `Date`, `Deadline`, `Priority`, `Labels (Tags)`.
  - Each meta row uses a clean, minimal icon + value layout.

## 🔍 Advanced Filtering Hub (TasksPage.tsx)

### 1. Expandable Filter Bar
- **Interaction**: Clicking the "Filter" button in the header slides down a dedicated filter panel.
- **Layout**: Horizontal or grid-based row of multi-selects (Status, Priority, Project, Tag).
- **UX**: Shows "active filters" as pills in the bar for easy removal.

## 🛠️ Creation & Audit UX

### 1. Searchable Inline Creation
- **Pattern**: "Press Enter to Create" in Project and Tag selectors.
- **Component**: Update `new-task-modal.tsx` and sidebar's Project add flow.

### 2. Audit Trail Humanization
- **Mapping Fix**:
  - Update log parser to handle status changes gracefully.
  - "Completed task 'X'" instead of raw detail strings.
  - "Marked 'X' as done".

## 🚦 Verification Gates
- [ ] Sidebar toggle matches the "boxed" visual pattern from Image 2.
- [ ] "Task Studio" is renamed to "Tasks" everywhere.
- [ ] Clicking a task opens the side-drawer with the 2-column detail layout.
- [ ] Filter bar correctly slides down and allows multi-select filtering.
- [ ] Audit trail logs read like natural human language.

---
*Generated: 2026-05-11*
