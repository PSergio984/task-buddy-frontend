# Plan Phase 4.1: Advanced UI/UX & Feature Refinement

Elevate Task Buddy to a professional-grade specialized workspace with a high-velocity sidebar, premium task drawer, and advanced filtering.

## 🛠️ Task 1: Sidebar Evolution (High-Velocity Nav)
**Objective**: Transform the sidebar into a structural, TickTick-inspired navigation hub.

- [ ] **Boxed Toggle**: Update `src/components/sidebar.tsx`
  - [ ] Implement a boxed container for the `PanelLeftOpen`/`PanelLeftClose` toggle at the top right.
  - [ ] Apply `bg-background/80 backdrop-blur-md border border-border/50` styling.
- [ ] **Smart Lists & Reorg**:
  - [ ] Create a `Smart Lists` section at the top: `Today`, `Next 7 Days`, `Inbox`.
  - [ ] **Feature**: Clicking these should set `activeSidebarFilter` in `FilterContext` to `today`, `upcoming`, or `inbox`.
  - [ ] Rename "Task Studio" link to "Tasks".
- [ ] **Tag Icons**:
  - [ ] Add a `Tag` Lucide icon to the "Focus Tags" header.
  - [ ] Adjust spacing for better density.

## 📋 Task 2: Premium Task Detail Drawer
**Objective**: Replace task-view modals with a professional side-drawer for deep focus.

- [ ] **New Component**: `src/components/task-detail-drawer.tsx`
  - [ ] Use `Sheet` from shadcn/ui (if available) or a custom Framer Motion `motion.div` right-side panel.
  - [ ] **Layout**:
    - Left (65%): Title (editable), Description (textarea/markdown), Subtasks (list), Comments.
    - Right (35%): Meta fields (Status, Project, Deadline, Priority, Tags) in a clean vertical list.
- [ ] **Integration**:
  - [ ] Update `TasksPage.tsx` and `Dashboard.tsx` to open this drawer when a task card is clicked.

## 🔍 Task 3: Advanced Filtering Hub
**Objective**: Implement a Linear-style expandable filter bar for precise task slicing.

- [ ] **Component**: `src/pages/TasksPage.tsx`
  - [ ] Add an "Expandable Filter Bar" component below the main header.
  - [ ] Toggle visibility via the "Filter" button.
  - [ ] **Controls**: Multi-select dropdowns for Status, Priority, Project, and Tags.
  - [ ] **UX**: Show active filter "pills" inside the bar that are removable with one click.

## 🛠️ Task 4: Inline Search-and-Create Flow
**Objective**: Remove friction from categorization by allowing creation during selection.

- [ ] **Combobox Refactor**:
  - [ ] Update Project and Tag selectors in `new-task-modal.tsx` and `TaskDetailDrawer` to use a `Command` / `Combobox` pattern.
  - [ ] **Logic**: If a search term doesn't match an existing item, show a `+ Create "{term}"` option.
  - [ ] Handle backend creation inline and update local state/context.

## 📜 Task 5: Audit Trail Humanization
**Objective**: Clean up the "log slop" to make history actually readable.

- [ ] **Component**: `src/components/audit-trail.tsx`
  - [ ] Update the `getActionLabel` mapping.
  - [ ] **Logic**: Specifically detect "status update to completed" and return "Marked as done" or "Completed task".
  - [ ] Strip out redundant "Updated task X" prefixes if the action is already descriptive.

## 🚦 Verification Loop
- [ ] **Sidebar**: Verify toggle visual matches Image 2 and renaming to "Tasks" is consistent.
- [ ] **Drawer**: Click a task, verify the 2-column drawer slides in smoothly.
- [ ] **Filters**: Apply multiple filters (e.g. High Priority + Project A), verify the list updates.
- [ ] **Inline Add**: Type a new tag name in the task drawer, hit enter, verify it creates and assigns.
- [ ] **Build Check**: `npm run build` must succeed.

---
*Status: Ready for execution*
