---
phase: 02-advanced-tasks
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
  - app/config.py
  - app/security.py
  - app/api/routers/user.py
  - package.json
  - src/lib/query-client.ts
  - src/App.tsx
---

<objective>
Establish the architectural foundation for Phase 2 by provisioning Redis for the backend and integrating TanStack Query for the frontend. This plan ensures that the application is ready for secure session management and optimized data fetching.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-advanced-tasks/02-CONTEXT.md
</context>

<tasks>
1. [Backend] Add `redis:7-alpine` service to `docker-compose.yml`.
2. [Backend] Update `app/config.py` to include `REDIS_URL` with a default of `redis://localhost:6379/0`.
3. [Backend] Implement `blacklist_token(token, expires_in)` in `app/security.py` using `redis-py`.
4. [Backend] Update `get_current_user` in `app/security.py` to check the Redis blacklist.
5. [Backend] Modify `logout` in `app/api/routers/user.py` to blacklist the current token upon successful logout.
6. [Frontend] Install `@tanstack/react-query` and `@tanstack/react-query-devtools`.
7. [Frontend] Create `src/lib/query-client.ts` to initialize the global `QueryClient`.
8. [Frontend] Wrap the application in `QueryClientProvider` within `src/App.tsx`.
</tasks>

<success_criteria>
- Redis service is healthy and reachable by the backend.
- Logged-out tokens are verifiable in Redis with appropriate TTL.
- Subsequent requests with a blacklisted token return 401 Unauthorized.
- TanStack Query DevTools are visible in development mode.
</success_criteria>

<output>
After completion, create `.planning/phases/02-advanced-tasks/02-01-SUMMARY.md`
</output>
