---
phase: 02-advanced-tasks
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - src/components/layout/main-layout.tsx
  - src/App.tsx
  - src/hooks/useApi.ts
  - src/hooks/useTasks.ts
  - src/hooks/useStats.ts
---

<objective>
Refactor the frontend data layer to use TanStack Query and decompose the "MainLayout" to eliminate component bloat and improve state management.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-advanced-tasks/02-CONTEXT.md
</context>

<tasks>
1. [Frontend] Create `src/hooks/useTasks.ts` using `useQuery` and `useMutation` from TanStack Query.
2. [Frontend] Create `src/hooks/useStats.ts` using `useQuery` from TanStack Query.
3. [Frontend] Decompose `MainLayout.tsx` by extracting the sidebar logic into a focused component if needed.
4. [Frontend] Migrate `MainLayout` state (tasks, stats) to the TanStack Query cache.
5. [Frontend] Refactor `TaskCard` interactions (Toggle Complete, Delete) to use new Query mutations with optimistic updates.
6. [Frontend] Remove manual `useEffect` fetch logic from `useApi.ts` (keeping only the axios instance and raw api functions).
</tasks>

<success_criteria>
- Data fetching is handled by TanStack Query.
- `MainLayout.tsx` is reduced in size by at least 30%.
- Task completion toggle feels instantaneous (Optimistic Updates).
- Refreshing the dashboard does not cause flickering, leveraging cached data.
</success_criteria>

<output>
After completion, create `.planning/phases/02-advanced-tasks/02-02-SUMMARY.md`
</output>
