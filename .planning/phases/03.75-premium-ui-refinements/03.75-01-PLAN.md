---
phase: 3.75
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: 
  - src/components/sidebar.tsx
  - src/components/dashboard.tsx
  - src/components/new-task-modal.tsx
  - src/pages/TaskDetailPage.tsx
autonomous: true
requirements: ["PREMIUM-UI"]
user_setup: []

must_haves:
  truths:
    - "Sidebar correctly scopes tasks to the selected workspace"
    - "Dashboard displays only specified filtered views (All, Today, Tomorrow, This Week)"
    - "Create Task modal includes time selection and a modernized UI"
  artifacts:
    - path: "src/components/sidebar.tsx"
      provides: "Collapsible sidebar with specific icon retention and scoped click handlers"
    - path: "src/components/dashboard.tsx"
      provides: "Simplified task view logic"
    - path: "src/components/new-task-modal.tsx"
      provides: "Modernized create task modal with time selection"
  key_links: []
---

<objective>
Implement the premium UI refinements for Phase 3.75 based on the user's design context.

Purpose: Modernize the Task Buddy interface, aligning it with premium visual standards and the user's specific layout requirements (from the 3.75-CONTEXT and UI-SPEC).
Output: Updated Sidebar, Dashboard, and Create Task components with correct visual styles, simplified views, and correct behavior.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.75-premium-ui-refinements/03.75-CONTEXT.md
@.planning/phases/03.75-premium-ui-refinements/03.75-UI-SPEC.md
@src/components/sidebar.tsx
@src/components/dashboard.tsx
@src/components/new-task-modal.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refine Sidebar & Workspace Layout</name>
  <files>src/components/sidebar.tsx, src/components/layout/main-layout.tsx</files>
  <read_first>src/components/sidebar.tsx</read_first>
  <action>Update the Sidebar component to remove redundant logout/avatar icons. Adjust the collapse behavior to keep the collapse icon visible while hiding the main Task Buddy logo. Ensure that clicking a workspace in the sub-sidebar scopes the tasks to that specific workspace instead of navigating to the general dashboard. Add tag visualization (colored pills per UI-SPEC) within the sub-sidebar.</action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - Logout/avatar elements are removed from sidebar.tsx
    - Collapse icon remains visible on collapse state
    - Workspace click handlers correctly scope tasks without full navigation routing to general dashboard
  </acceptance_criteria>
  <done>Sidebar behaves as requested and matches context.</done>
</task>

<task type="auto">
  <name>Task 2: Simplify Dashboard Views</name>
  <files>src/components/dashboard.tsx</files>
  <read_first>src/components/dashboard.tsx</read_first>
  <action>Refactor the Dashboard component to only include the "All", "Due Today", "Due Tomorrow", and "Due This Week" categories. Remove any other redundant filters or task groupings that cluttered the previous dashboard. Apply the glassmorphic / subtle shadow styles from the UI-SPEC (white/dark cards).</action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - Dashboard only renders the four specified task categories.
    - Card styling follows UI-SPEC.
  </acceptance_criteria>
  <done>Dashboard renders the correct simplified task filters.</done>
</task>

<task type="auto">
  <name>Task 3: Modernize Create/Edit Task UI</name>
  <files>src/components/new-task-modal.tsx, src/pages/TaskDetailPage.tsx</files>
  <read_first>src/components/new-task-modal.tsx</read_first>
  <action>Update the text from "manifest task" to "Create task". Add a time selection input (using Shadcn UI inputs or a native time picker if more appropriate) alongside the date picker. Remove the grey background entirely; use pure white or dark cards with subtle shadows (`shadow-sm`, `bg-white` or `bg-zinc-900` per UI-SPEC). Apply these same visual updates to the TaskDetailPage/Edit form.</action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - "manifest task" text is removed, replaced with "Create task"
    - Time selection is available in the form
    - Form has a pure white/dark background with subtle shadows, not grey.
  </acceptance_criteria>
  <done>Create Task modal and Edit view are modernized with time selection.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds
- [ ] No typecheck errors
- [ ] Visual verification of sidebar, dashboard, and modal via `npm run dev`
</verification>

<success_criteria>
- All tasks completed successfully without regressions.
- The UI strictly adheres to the 3.75-UI-SPEC and 3.75-CONTEXT decisions.
</success_criteria>

<output>
After completion, create `.planning/phases/03.75-premium-ui-refinements/03.75-01-SUMMARY.md`
</output>
